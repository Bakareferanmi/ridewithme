export const APP_NAME = "RideWithMe";

export interface Vehicle {
  id: string;
  make: string;
  model: string;
  year: number;
  price: number;
  listingType: "buy" | "rent" | "lease" | "auction";
}
